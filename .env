# Base URL of the backend API (currently splashmain's deployed Next.js app).
# This is the ONLY place that domain should ever be referenced — every API
# call in this app goes through src/lib/api.ts, which reads this value.
# When the backend is eventually extracted to its own project, only this
# line needs to change.
VITE_API_BASE_URL=
