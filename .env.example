# Base URL of the backend API (splashmain's deployed Next.js app — this is
# the permanent backend for this app, not a temporary arrangement).
# This is the ONLY place that domain should ever be referenced — every API
# call in this app goes through src/lib/api.ts, which reads this value.
VITE_API_BASE_URL=https://splashmain.vercel.app
